#include <stdio.h>

int main()
{
    /*
    1. Write a program, in which you take 3 inputs from user and apply the
    operations, Add, subtract, divide and multiply

    2. Write a program to interchange values of two operands a and b.
    */

    // int a = 4, b = 6;

    // a += b;
    // b += a;

    // printf("Interchange value of a %d\n", a);
    // printf("Interchange value of b %d", b);

    /* 3. Write a program for calculating the percentage of a student, by taking
    input of 5 subject’s marks and each course have max 100 marks.*/

    // int sub1, sub2, sub3, sub4, sub5, total;

    // float percentage;

    // printf("Enter marks of Subject Math (out of 100): ");
    // scanf("%d", &sub1);

    // printf("Enter marks of Subject Science (out of 100): ");
    // scanf("%d", &sub2);

    // printf("Enter marks of Subject English (out of 100): ");
    // scanf("%d", &sub3);

    // printf("Enter marks of Subject Computer (out of 100): ");
    // scanf("%d", &sub4);

    // printf("Enter marks of Subject Pakistan Studies (out of 100): ");
    // scanf("%d", &sub5);

    // total = sub1 + sub2 + sub3 + sub4 + sub5;

    // percentage = (float) total / 500 * 100; 

    // printf("Total Marks = %d\n", total);
    // printf("Percentage = %.2f%%", percentage);


    /* 4. Write a program to print the size of char, float, double and long double
    data types in C.*/

    // printf("Size of char        : %lu byte\n", sizeof(char));
    // printf("Size of float       : %lu bytes\n", sizeof(float));
    // printf("Size of double      : %lu bytes\n", sizeof(double));
    // printf("Size of long double : %lu bytes\n", sizeof(long double));


    /*
    5. 
    */


}