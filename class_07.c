#include <stdio.h>

int main() {
    // char choices;
    // puts("===========================================");
    // puts("Choose Your Options:");

    // printf("1. Swap Two Values using Third Variable\n");
    // printf("2. Swap Two Values without using Third Variable\n");
    // printf("3. Size OF Data Types:\n");

    // puts("===========================================");
    // printf("Enter a character of your program: ");
    // scanf("%c", &choices);

    // if(choices == '1'){
    //     int a = 2, b = 4, c = 6;
    //     printf("Before Swaping The Values: \na = %d\n, b = %d\n, c = %d\n", a, b, c);

    //     a = b;
    //     b = c;
    //     c = a;

    //     printf("After Swaping The Values: \na = %d\n, b = %d\n, c = %d\n", a, b, c);
    // }
    // else if(choices == '2') {
    //     int a = 2, b = 4;

    //     printf("Before Swaping The Values: \na = %d\n, b = %d\n", a, b);

    //     a = a ^ b;
    //     b = a ^ b;
    //     a = a ^ b;

    //     printf("After Swaping The Values: \na = %d\n, b = %d\n", a, b);
    // }
    // else if(choices == '3') {
    //     printf("size of char is: char = %lu bytes\n", sizeof(char));
    //     printf("size of int is: int = %lu bytes\n", sizeof(int));
    //     printf("size of float is: float = %lu bytes\n", sizeof(float));
    //     printf("size of double is: double = %lu bytes", sizeof(double));
    // }

    // Write a c program that prints the time using ASII Character

    // char ch;
    // int hour;

    // printf("Enter the character between (0 - 9): ");
    // scanf("%c", &ch);

    // hour = ch - '0';

    // if(hour >= 0 && hour <= 11) {
    //     printf("Good Morning its %d AM", hour);
    // }
    // else if(hour >= 12 && hour <= 15) {
    //     printf("Good Afternoon its %d PM", hour);
    // }
    // else if(hour >= 16 && hour <= 18) {
    //     printf("Good Evening %d its PM");
    // }


    // int num = 1443, reversed = 0;

    // reversed = (num % 10) * 1000 + ((num / 10) % 10) * 100 + ((num / 100) % 10) * 10 + (num / 1000);

    // printf("Reverse Number: %d\n", reversed);
}